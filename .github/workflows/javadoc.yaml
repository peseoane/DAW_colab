name: Deploy Javadoc

on:
  push:
    branches:
      - master
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy JavaDoc 🚀
        uses: MathieuSoysal/Javadoc-publisher.yml@v2.0.4
        with:
          GITHUB_TOKEN: ${{ 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}}
          javadoc-branch: javadoc
          java-version: 17
          target-folder: javadoc 